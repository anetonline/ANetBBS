"""anetbbs-cfg: standalone terminal admin tool (SCFG / mystic -cfg style).

Run with `python -m anetbbs.cfg` or the installed `anetbbs-cfg` command.
Talks to the same database as the web/BBS processes via the normal
`create_app()` app-context pattern -- see anetbbs/cfg/app.py.
"""
